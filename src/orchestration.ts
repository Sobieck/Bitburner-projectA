/** @param {NS} ns **/

import { NS } from "@ns";

export async function main(ns: NS) : Promise<void> {

    // script-to-run runs very first thing

    // const scripts = await ns.fileExists("/data/scripts-to-run.txt")

    // LOOOP on scripts to run
    // await ns.sleep(700)



    // sleep 1000 -> then orchestrate
    // orchestrator always ends outside of the loop
}